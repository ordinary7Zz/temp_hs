DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=backup_system

# 可选：通过 Unix Socket 连接（macOS/Oracle MySQL 默认 /tmp/mysql.sock）
# 设置后将优先使用 socket 连接
# DB_SOCKET=/tmp/mysql.sock